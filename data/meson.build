install_data('rpcc.ui', install_dir: ui_dir)
  i18n.merge_file(input: 'rpcc.desktop.in',
      output: 'rpcc.desktop',
      type: 'desktop',
      po_dir: '../po',
      install: true,
      install_dir: desktop_dir
    )

